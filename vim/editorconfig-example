# Set this to true if you don't want editorconfig to look further in the parents
root = true

# Config for every file
[*]
charset = utf-8
indent_style = tab
indent_size = 4 # Don't specify this if you want everybdy to see it in their prefered tab width
end_of_line = lf
insert_final_newline = true

# Config for JSON files
[*.json]
indent_size = 2

# Config for JS and PY files
[*.{js,py}]
trim_trailing_whitespace = true # Remove whitespaces before newlines
